import 'package:flutter/material.dart';

class AcceptPayments extends StatelessWidget {
  const AcceptPayments({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Accept Payments'),
      ),
    );
  }
}
